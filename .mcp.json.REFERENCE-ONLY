{
  // MCP Servers — external tool providers
  // This file goes at ~/.claude/.mcp.json
  // Or add servers via CLI: claude mcp add --transport http <name> <url>
  "mcpServers": {
    // Salesforce SE Grounding Service — verified product knowledge, competitive intel, field insights
    // Requires ZScaler VPN active
    // Provides: grounding_search, grounding_search_narrowed, grounding_bm25, kg_lookup, kg_traverse, kg_entity_detail, kg_stats
    "salesforce-grounding": {
      "type": "url",
      "url": "https://grounding.35-186-235-101.sslip.io/mcp"
    }
  }
}
